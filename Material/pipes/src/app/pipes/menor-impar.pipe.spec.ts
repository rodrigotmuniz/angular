import { MenorImparPipe } from './menor-impar.pipe';

describe('MenorImparPipe', () => {
  it('create an instance', () => {
    const pipe = new MenorImparPipe();
    expect(pipe).toBeTruthy();
  });
});
