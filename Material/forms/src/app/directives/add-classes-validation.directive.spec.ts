import { AddClassesValidationDirective } from './add-classes-validation.directive';

describe('AddClassesValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassesValidationDirective();
    expect(directive).toBeTruthy();
  });
});
